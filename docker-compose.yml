version: "3.8"

services:
  ladeschweinle:
    build: .
    ports:
      - "1337:1337"
    environment:
      HOST: 0.0.0.0
      PORT: 1337
    volumes:
      - ./data:/app/data
    restart: unless-stopped
